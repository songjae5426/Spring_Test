<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.songjae.spring.test.mybatis.repository.Real_estateRepository">
 	<select id="selectReal_estate" resultType="com.songjae.spring.test.mybatis.domain.Real_estate" parameterType="int">
 		SELECT
 			  `id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM `real_estate`
 		WHERE `id` = #{id}
 	</select>
 	
 	<select id="selectReal_estateRent" resultType="com.songjae.spring.test.mybatis.domain.Real_estate" parameterType="int">
 		SELECT
 			  `id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM `real_estate`
 		<![CDATA[
 		WHERE `rentPrice` < #{rent}
 		]]>
 	</select>
 	
 	<select id="selectReal_estateRentComplexConditions" resultType="com.songjae.spring.test.mybatis.domain.Real_estate" parameterType="Map">
 		SELECT
 			  `id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM `real_estate`
 		<![CDATA[
 		WHERE `area` >= #{area} AND `price` <= #{price}
 		]]>
 	</select>
 </mapper>